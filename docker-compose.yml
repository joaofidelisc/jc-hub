version: '3.8'

services:
  postgres:
    image: ankane/pgvector:latest
    container_name: jc-hub-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-admin}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-admin}
      POSTGRES_DB: ${POSTGRES_DB:-jcbusiness}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - jc-hub-network

  redis:
    image: redis:7-alpine
    container_name: jc-hub-redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    networks:
      - jc-hub-network

  backend:
    image: jc-hub-backend
    build: 
      context: ./backend
    container_name: jc-hub-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_DB: ${REDIS_DB:-1}
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app
    networks:
      - jc-hub-network

  worker:
    image: jc-hub-worker
    build: 
      context: ./backend
    container_name: jc-hub-worker
    restart: unless-stopped
    command: python worker.py
    env_file:
      - .env
    environment:
      POSTGRES_HOST: postgres
      REDIS_HOST: redis
      REDIS_PORT: ${REDIS_PORT:-6379}
      REDIS_DB: ${REDIS_DB:-1}
    depends_on:
      - postgres
      - redis
    volumes:
      - ./backend:/app
    networks:
      - jc-hub-network

  frontend:
    image: node:18-alpine
    container_name: jc-hub-frontend
    restart: unless-stopped
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0 --port 3000"
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
    working_dir: /app
    networks:
      - jc-hub-network

networks:
  jc-hub-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
